uploads/avatars/*
!uploads/avatars/.gitkeep
config/mail_config.php
init-db/

# ห้ามขึ้น GitHub: มี BREVO_API_KEY และรหัสผ่านอีเมลจริงอยู่ในนี้ (docker-compose.yml อ่านค่าจากไฟล์นี้แทน)
.env

# ห้ามขึ้น GitHub: มีข้อมูลสุขภาพนักเรียนจริง (PII) อยู่ในนี้ (ใช้กับ docker-compose.offline.yml)
db/
hospital-project.tar
backups/
